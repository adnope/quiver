# Quiver Frontend

Vite + React + TypeScript single-page application for the Quiver operator UI.

## Scripts

```bash
npm run dev
npm run typecheck
npm run lint
npm run test
npm run build
```

The production build writes static assets to `dist/`. Later implementation phases embed those assets into the Go binary.
